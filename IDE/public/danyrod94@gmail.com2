program
  
 blocks
    define number fibonacci(number a)
        if (a < 2)
            return a;
        else
            return fibonacci(a - 1) + fibonacci(a - 2);
        endif
    enddefine
 endblocks


 start
   variables
        manynumbers xd[5];
        number a = 7, b = 1, c;
   endvariables

   a = fibonacci(a);
   
    while(b < 6)
        display(b);
        b = b + 1;
    endwhile

    display(a);

 finish
 endprogram