program
  
 


 start
   variables
        manynumbers xd[5];
        number a = 7, b = 1, c;
        flag meme = false;
   endvariables

   if (not meme)
        a =3;
    else
        b=4+56;
        if(a > 7)
            a = 14;
        endif
        b = b + 3;
    endif

    b = 1;
    

    while(b < 4)
        if(b > 2)
            a = a + 3;
        endif
        b = b + 1;
    endwhile

    display("El resultado de este meme es ", b + 1 , "jejetl");
   

 finish
 endprogram